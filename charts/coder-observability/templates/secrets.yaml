apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: {{ .Release.Name }}.aws
  namespace: {{ .Release.Namespace }}
  {{- with .Values.extra.secretStore.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.extra.secretStore.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  provider:
    aws:
      service: SecretsManager
      region: {{ .Values.extra.secretStore.aws.region }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Release.Name }}.secrets
  namespace: {{ .Release.Namespace }}
  {{- $postgresExporter := (index .Values "postgres-exporter") }}
  {{- with $postgresExporter.passwordSecret.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $postgresExporter.passwordSecret.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  secretStoreRef:
    name: {{ .Release.Name }}.secrets
    kind: SecretStore
  refreshPolicy: {{ $postgresExporter.passwordSecret.refreshPolicy }}
  refreshInterval: {{ $postgresExporter.passwordSecret.refreshInterval }}
  target:
    name: {{ $postgresExporter.passwordSecret.name }}
    creationPolicy: Owner
    deletionPolicy: Retain
  dataFrom:
    - extract:
        conversionStrategy: Default
        decodingStrategy: None
        key: {{ $postgresExporter.passwordSecret.secretArn }}
        metadataPolicy: None