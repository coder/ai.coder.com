apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: {{ .Release.Name }}.aws
  namespace: {{ .Release.Namespace }}
  {{- with .Values.extra.secretStore.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.extra.secretStore.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  provider:
    aws:
      service: SecretsManager
      region: {{ .Values.extra.secretStore.aws.region }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Release.Name }}.secrets
  namespace: {{ .Release.Namespace }}
  {{- $postgresExporter := (index .Values "postgres-exporter") }}
  {{- with $postgresExporter.config.datasource.passwordSecret.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $postgresExporter.config.datasource.passwordSecret.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  secretStoreRef:
    name: {{ .Release.Name }}.aws
    kind: SecretStore
  refreshPolicy: {{ $postgresExporter.config.datasource.passwordSecret.refreshPolicy }}
  refreshInterval: {{ $postgresExporter.config.datasource.passwordSecret.refreshInterval }}
  target:
    name: {{ $postgresExporter.config.datasource.passwordSecret.name }}
    creationPolicy: Owner
    deletionPolicy: Retain
  dataFrom:
    - extract:
        conversionStrategy: Default
        decodingStrategy: None
        key: {{ $postgresExporter.config.datasource.passwordSecret.secretArn }}
        metadataPolicy: None