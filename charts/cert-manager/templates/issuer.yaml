{{- if .Values.extra.defaultIssuer.enable }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.extra.defaultIssuer.name }}
  {{- with .Values.extra.defaultIssuer.labels }}
  labels: 
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.extra.defaultIssuer.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
    "helm.sh/hook": post-install,post-upgrade
  {{- end }}
spec:
  acme:
    privateKeySecretRef:
      name: {{ .Values.extra.defaultIssuer.acme.privateKeySecretRef.name }}
    server: {{ .Values.extra.defaultIssuer.acme.server }}
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              key: {{ .Values.extra.defaultIssuer.acme.cloudflare.apiTokenSecretRef.key }}
              name: {{ .Values.extra.defaultIssuer.acme.cloudflare.apiTokenSecretRef.name | lower }}
            email: {{ .Values.extra.defaultIssuer.acme.cloudflare.email }}
{{- end }}